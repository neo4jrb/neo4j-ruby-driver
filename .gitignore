.rspec_status
Gemfile.lock
.ruby-version
.idea/
pkg/
.claude/settings.local.json

*.jar
neo4j-ruby-driver_jars.rb

# Populated by testkit's _ensure_image() before docker build
# (CAs/ from testkit's tests/tls/certs/driver/trusted,
#  CustomCAs/ from .../custom). Transient build context.
testkit/CAs/
testkit/CustomCAs/
# JRuby/jbundler build artifacts (generated; not part of the gem)
testkit-backend/gems.locked
testkit-backend/.mvn/
