"""Integration test entry point.

Testkit's orchestrator starts the backend (backend.py) and then runs
testkit's own Python integration tests against it. This script is a
placeholder for driver-specific integration steps — our RSpec suite
doubles as the driver-level integration check and lives in
unittests.py.
"""

if __name__ == "__main__":
    pass
